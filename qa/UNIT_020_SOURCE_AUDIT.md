# Unit 020 source audit

## Scope and reproducible identity

- Authority: `authority/upstream/AlgebraicTopology2019-b947ad2e9f9e301bfe24590a9db653bc54fa1a53/Notes.tex`.
- Upstream commit: `b947ad2e9f9e301bfe24590a9db653bc54fa1a53`.
- Audited active span: physical source lines **3948–4345**, exactly 398 LF-joined
  lines, 17,645 UTF-8 bytes, SHA-256
  `1fa7d0ea4ecd567ae8975da5b9b41495a1757913942102f223d1234168366e88`.
- Raw pre-marker witness: lines **3948–4346**, 399 lines, 17,657 bytes, SHA-256
  `6af488776f936d7a3ef17a30a8af94e6955df91e3a3057b92b048e1b38ca1917`.
- Line 4346 opens the next lecture's `rem`; line 4347 contains the Lecture 21
  marker. It is deferred intact to Unit 21, so no 4346–4350 content is split
  into this unit.

## Source census

The active span contains 8 `example` environments, 4 formal `ex` environments
(5 source prompts because the first has two enumerate parts), 2 definitions,
2 lemmas, 2 proofs, 4 remarks, 11 margin notes, 7 TikZ figures, 6 labels,
8 display-math blocks, 2 `align*` blocks, 1 `align` block, and 3 `cases`
blocks. It has no external graphics, `input`, `include`, or Xy-pic dependency.

## Translation and accessibility decisions

The reader preserves source order, formulas, identifiers, attribution, and
the CC BY 4.0/non-endorsement notice. All seven figures are represented by
centered semantic descriptions/tables; no mathematical direction depends on
colour or margin position. The source's eleven margin notes are promoted into
ordinary reading-order callouts. The source's function modules `R^S` are
explicitly described as all functions (products), not finite-support modules.

## Mathematical/source corrections recorded in the unit

1. The triangle graph uses an explicit row/column convention and the correct
   transpose matrix, with kernel generated by `A+B+C`.
2. The malformed source `0\\ZZ^4` is rendered as a typed arrow to
   `\\mathbb Z^4`.
3. Graph claims and Euler-characteristic statements are restricted to finite
   data where the stated rank/count argument is valid.
4. The face-map identities are expanded with all six terms, proving
   `delta_1 delta_0 = 0`; the duplicated/missing source composition is noted.
5. The source's Klein-bottle entry `d_1^2(f_2)=e_2` is corrected to `e_3`,
   while `d_2^2(f_2)=e_2` is retained. The resulting Smith form is
   `diag(1,2)`, so the cokernel and `H^2` are `Z/2`.
6. The cohomology quotient is typed as kernel modulo image, and functoriality
   is proved rather than asserted.
7. Source spelling/grammar defects at the recorded locators are corrected in
   Indonesian; source-audit callouts retain the provenance of each change.

## Mastery and provenance closure

Six edition-original mastery exercises each have a stable problem, hint, and
complete solution (`mcheck-001..006`, `hint-001..006`, `sol-001..006`). The
unit explicitly credits **OpenAI Codex gpt-5.6-sol, Ultra** as process
provenance while preserving Roberts's authorship, human direction, CC BY 4.0,
and non-endorsement. This audit is read-only evidence; it does not alter the
upstream authority.

